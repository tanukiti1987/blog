machine:
  ruby:
    version: '2.4'

dependencies:
  cache_directories:
    - 'vendor/bundle'
  override:
    - sudo pip install awscli
    - gem install bundler
    - bundle -j8 --path=vendor/bundle

test:
  override:
    - echo "no tests"

deployment:
  production:
    branch: master
    commands:
      - ./scripts/deploy.sh
